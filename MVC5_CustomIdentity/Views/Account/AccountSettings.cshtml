@model MVC5_CustomIdentity.Models.AccountSettingsViewModel

<h2>Account Settings</h2>
<p class="text-danger">@ViewBag.ErrorMessage</p>

@using (Html.BeginForm("AccountSettings", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()

    if (ViewBag.showForm == null || ViewBag.showForm == true)
    {

        <div class="form-group">
            @Html.LabelFor(m => m.Email, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.Email)


                @Html.HiddenFor(model => model.OldEmail, new { Value = @Model.Email })

            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" class="btn btn-default" value="Save" />
            </div>
        </div>
    }
    else
    {
        <p class="text-success">@ViewBag.StatusMessage</p>

    }

}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
